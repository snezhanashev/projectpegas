services:
  app:
    build:
      context: ../
      dockerfile: artifacts/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - REDIS_HOST=cache
      - NODE_ENV=production
    depends_on:
      - db
      - cache
    networks:
      - pega-net
